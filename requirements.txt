"confluent-kafka\npandas" 
# installation de la librairie confluent-kafka(pour faciliter la connexion) et pandas (pour la manipulation des données)
# installation of the confluent-kafka library (to facilitate the connection) and pandas (for data manipulation)
confluent-kafka==1.9.2 
pandas==2.0.3  
